import React from 'react';
import { StyledMain } from './styles';
import { Table } from '../../blocks/table/table';

export const Main = () => {
    return (
        <StyledMain>
            <Table />
        </StyledMain>
    );
}
